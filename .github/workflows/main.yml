name: Run Reality Server
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Download Xray and Setup Config
        run: |
          # دانلود نسخه کاملاً رسمی و پایدار Xray-core
          curl -L -o xray.zip https://github.com/XTLS/Xray-core/releases/download/v1.8.23/Xray-linux-64.zip
          unzip xray.zip -d ./xray_bin
          chmod +x ./xray_bin/xray
          
          # ساخت کانفیگ کاملاً استاندارد REALITY (بدون فلو اضافی)
          echo '{
            "log": { "loglevel": "warning" },
            "inbounds": [{
              "port": 8080,
              "protocol": "vless",
              "settings": {
                "clients": [{ "id": "9f4e2468-b715-4c31-b3b3-85764b8d781a" }],
                "decryption": "none"
              },
              "streamSettings": {
                "network": "tcp",
                "security": "reality",
                "realitySettings": {
                  "show": true,
                  "dest": "www.docker.com:443",
                  "xver": 0,
                  "serverNames": ["www.docker.com"],
                  "privateKey": "uGv_VjS9_3W9XpOfM6b8G3f_Xm1n7Z4v8_Wp2x4t5mE",
                  "shortIds": ["66"]
                }
              }
            }],
            "outbounds": [{ "protocol": "freedom" }]
          }' > ./config.json

      - name: Start Server & Create Tunnel
        run: |
          # اجرای سرور
          ./xray_bin/xray run -c ./config.json &
          
          # اجرای تونل tmate
          curl -L https://github.com/tmate-io/tmate/releases/download/2.4.0/tmate-2.4.0-static-linux-amd64.tar.xz | tar xJ
          ./tmate-2.4.0-static-linux-amd64/tmate -F
